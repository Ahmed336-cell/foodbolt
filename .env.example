# Copy to assets/env/.env for local runs (gitignored secrets stay out of git).
# Prefer project-root `.env` — app merges it over assets on startup.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=your_anon_key
BRANCH_KEY=
INVITE_BASE_URL=foodrush://join
USE_MOCKS=false
